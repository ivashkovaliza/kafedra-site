kafedra-site
-----------------------------------

###Install Kafedra-site:

$ npm install

###Build the project in development mode:

$npm run dev

###Build the project in production mode:

$npm run prod

